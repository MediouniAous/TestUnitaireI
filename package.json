{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint  --ignore-path .gitignore .",
    "format": "prettier --write --ignore-path .gitignore \"**/*.+(js|json)\"",
    "check-format": "prettier --write --ignore-path .gitignore --list-different \"**/*.+(js|json)\"",
    "validate": "npm run check-format && npm run lint "
  },
  "lint-staged": {
    "*.+(js|jsx)": [
    "eslint"],
    "**/*.+(js|json|jsx)":[
      "prettier--write",
      "git add"
    ]
},
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eslint": "^7.21.0",
    "eslint-config-prettier": "^8.1.0",
    "husky": "^5.1.3",
    "prettier": "^2.2.1"
  },
  "devDependencies": {
    "lint-staged": "^10.5.4"
  }
}
